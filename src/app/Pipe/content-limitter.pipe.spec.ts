import { ContentLimitterPipe } from './content-limitter.pipe';

describe('ContentLimitterPipe', () => {
  it('create an instance', () => {
    const pipe = new ContentLimitterPipe();
    expect(pipe).toBeTruthy();
  });
});
